# Configuration pour développement local
# Sur le serveur de production, les variables sont définies directement dans /root/docker-compose.yml

# Configuration de l'application
APP_NAME=investing-calendar-api
APP_VERSION=1.0.0
ENVIRONMENT=development

# Configuration du serveur
HOST=0.0.0.0
PORT=8001

# Configuration des logs
LOG_LEVEL=INFO

# Configuration du timezone par défaut (55 = Europe/London)
DEFAULT_TIMEZONE=55

# Configuration Uvicorn (nombre de workers)
WORKERS=4

# Note: Sur le serveur de production, l'API est accessible via:
# https://myscrapers.srv842470.hstgr.cloud
# La configuration Traefik est gérée dans /root/docker-compose.yml

# ============================================
# CONFIGURATION DU SERVICE D'UNIFICATION
# ============================================

# URL de votre serveur Ollama privé
# Par défaut : http://localhost:11434
# Pour Docker : http://host.docker.internal:11434
# Pour serveur distant : http://votre-serveur-ollama:11434
OLLAMA_URL=http://31.97.53.106:11434

# Modèle Ollama pour les embeddings
# Assurez-vous que ce modèle est installé : ollama pull nomic-embed-text
OLLAMA_MODEL=nomic-embed-text

# Configuration PostgreSQL
# Les embeddings et mappings seront stockés dans PostgreSQL avec pgvector
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=unification
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
