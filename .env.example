# Configuration pour développement local
# Sur le serveur de production, les variables sont définies directement dans /root/docker-compose.yml

# Configuration de l'application
APP_NAME=investing-calendar-api
APP_VERSION=1.0.0
ENVIRONMENT=development

# Configuration du serveur
HOST=0.0.0.0
PORT=8001

# Configuration des logs
LOG_LEVEL=INFO

# Configuration du timezone par défaut (55 = Europe/London)
DEFAULT_TIMEZONE=55

# Configuration Uvicorn (nombre de workers)
WORKERS=4

# Note: Sur le serveur de production, l'API est accessible via:
# https://myscrapers.srv842470.hstgr.cloud
# La configuration Traefik est gérée dans /root/docker-compose.yml
